# Copy to .env only for local demonstration use. Do not commit .env.
# Provide actual values through the shell, a secret manager, or a local ignored file.

POSTGRES_PASSWORD=replace-for-local-use
SECURITY_JWT_SECRET=replace-with-a-long-random-local-secret

# Optional only when a Study Agent service requires an authenticated endpoint.
STUDY_AGENT_API_KEY=
